const name = "Amin";

const otherName = 'Yves';

const paragraph = "Ce caractères \" est un guillemet";

const text = 'Ce caractères " est un guillemet';

const firstName = "Amin";
const lastName = "NAIRI";

const fullName = firstName + " " + lastName;

// ALT-GR + 7 (backticks)
// Template string
const myName = `${firstName} ${lastName}!`;

console.log(myName);
