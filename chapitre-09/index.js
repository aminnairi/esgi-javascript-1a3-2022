const fruits = [
  "banana",
  "apple",
  "pear"
];

// Boucle sur tous les index du tableau
// for (let index = 0; index < fruits.length; index++) {
  // Accès à la valeur d'un index du tableau
// const fruit = fruits[index];

// console.log(fruit);
// }

// Boucle sur toutes les valeurs du tableau
// for (const fruit of fruits) {
// console.log(fruit);
// }

// Boucle sur les propriétés du tableaux (ses indexes)
// for (const index in fruits) {
  // Récupération de la valeur à un index donné
// const value = fruits[index];
// console.log(value);
// }

// Accès par son index
// console.log(fruits[0]);

// Propriété calculée (index)
// console.log(fruits["1"]);

// Modification d'une valeur à un index donné
// fruits[1] = "lemon";

// Ajout d'un élément à la fin du tableau (via méthode Array.prototype.push)
// fruits.push("strawberry");

// Ajout d'un élément à la fin du tableau (via index)
// fruits[fruits.length] = "strawberry";

// Suppression d'une valeur à un index (conserve la taille)
// delete fruits[2];
